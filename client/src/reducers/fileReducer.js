const initialState = {
  files:[],
  currentDir:null,

}

const fileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default fileReducer;
